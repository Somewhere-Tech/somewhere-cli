{
  "name": "swpx-verdict-api",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Verdict API for swpx/swpm — deploys to the `npm` somewhere.tech project (npm.somewhere.tech). NOT part of @somewhere-tech/cli.",
  "scripts": {
    "test": "node --test \"**/*.test.mjs\""
  }
}
